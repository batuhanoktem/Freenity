import { Instance, SnapshotOut, types } from "mobx-state-tree"
import { AuthorModel } from "../author"
import { FileModel } from "../file"

/**
 * Model description here for TypeScript hints.
 */
export const MessageModel = types
  .model("Message")
  .props({
    _id: types.string,
    user_id: types.maybeNull(types.string),
    __v: types.maybeNull(types.integer),
    attached: types.maybeNull(types.string),
    updated_at: types.maybeNull(types.Date),
    created_at: types.maybeNull(types.Date),
    date: types.maybeNull(types.string),
    article_zu: types.maybeNull(types.string),
    article_zh: types.maybeNull(types.string),
    article_yo: types.maybeNull(types.string),
    article_yi: types.maybeNull(types.string),
    article_xh: types.maybeNull(types.string),
    article_vi: types.maybeNull(types.string),
    article_uz: types.maybeNull(types.string),
    article_ur: types.maybeNull(types.string),
    article_uk: types.maybeNull(types.string),
    article_tr: types.maybeNull(types.string),
    article_tl: types.maybeNull(types.string),
    article_th: types.maybeNull(types.string),
    article_tg: types.maybeNull(types.string),
    article_te: types.maybeNull(types.string),
    article_ta: types.maybeNull(types.string),
    article_sw: types.maybeNull(types.string),
    article_sv: types.maybeNull(types.string),
    article_su: types.maybeNull(types.string),
    article_st: types.maybeNull(types.string),
    article_sr: types.maybeNull(types.string),
    article_sq: types.maybeNull(types.string),
    article_so: types.maybeNull(types.string),
    article_sn: types.maybeNull(types.string),
    article_sm: types.maybeNull(types.string),
    article_sl: types.maybeNull(types.string),
    article_sk: types.maybeNull(types.string),
    article_si: types.maybeNull(types.string),
    article_sd: types.maybeNull(types.string),
    article_ru: types.maybeNull(types.string),
    article_ro: types.maybeNull(types.string),
    article_pt: types.maybeNull(types.string),
    article_ps: types.maybeNull(types.string),
    article_pl: types.maybeNull(types.string),
    article_pa: types.maybeNull(types.string),
    article_ny: types.maybeNull(types.string),
    article_no: types.maybeNull(types.string),
    article_nl: types.maybeNull(types.string),
    article_ne: types.maybeNull(types.string),
    article_my: types.maybeNull(types.string),
    article_mt: types.maybeNull(types.string),
    article_ms: types.maybeNull(types.string),
    article_mr: types.maybeNull(types.string),
    article_mn: types.maybeNull(types.string),
    article_ml: types.maybeNull(types.string),
    article_mk: types.maybeNull(types.string),
    article_mi: types.maybeNull(types.string),
    article_mg: types.maybeNull(types.string),
    article_lv: types.maybeNull(types.string),
    article_lt: types.maybeNull(types.string),
    article_lo: types.maybeNull(types.string),
    article_lb: types.maybeNull(types.string),
    article_la: types.maybeNull(types.string),
    article_ky: types.maybeNull(types.string),
    article_ku: types.maybeNull(types.string),
    article_ko: types.maybeNull(types.string),
    article_kn: types.maybeNull(types.string),
    article_km: types.maybeNull(types.string),
    article_kk: types.maybeNull(types.string),
    article_ka: types.maybeNull(types.string),
    article_jw: types.maybeNull(types.string),
    article_ja: types.maybeNull(types.string),
    article_iw: types.maybeNull(types.string),
    article_it: types.maybeNull(types.string),
    article_is: types.maybeNull(types.string),
    article_ig: types.maybeNull(types.string),
    article_id: types.maybeNull(types.string),
    article_hy: types.maybeNull(types.string),
    article_hu: types.maybeNull(types.string),
    article_ht: types.maybeNull(types.string),
    article_hr: types.maybeNull(types.string),
    article_hmn: types.maybeNull(types.string),
    article_hi: types.maybeNull(types.string),
    article_haw: types.maybeNull(types.string),
    article_ha: types.maybeNull(types.string),
    article_gu: types.maybeNull(types.string),
    article_gl: types.maybeNull(types.string),
    article_gd: types.maybeNull(types.string),
    article_ga: types.maybeNull(types.string),
    article_fy: types.maybeNull(types.string),
    article_fr: types.maybeNull(types.string),
    article_fi: types.maybeNull(types.string),
    article_fa: types.maybeNull(types.string),
    article_eu: types.maybeNull(types.string),
    article_et: types.maybeNull(types.string),
    article_es: types.maybeNull(types.string),
    article_eo: types.maybeNull(types.string),
    article_en: types.maybeNull(types.string),
    article_el: types.maybeNull(types.string),
    article_de: types.maybeNull(types.string),
    article_da: types.maybeNull(types.string),
    article_cy: types.maybeNull(types.string),
    article_cs: types.maybeNull(types.string),
    article_co: types.maybeNull(types.string),
    article_ceb: types.maybeNull(types.string),
    article_ca: types.maybeNull(types.string),
    article_bs: types.maybeNull(types.string),
    article_bn: types.maybeNull(types.string),
    article_bg: types.maybeNull(types.string),
    article_be: types.maybeNull(types.string),
    article_az: types.maybeNull(types.string),
    article_ar: types.maybeNull(types.string),
    article_am: types.maybeNull(types.string),
    article_af: types.maybeNull(types.string),
    article: types.maybeNull(types.string),
    is_files_zu: types.maybeNull(types.string),
    is_files_zh: types.maybeNull(types.string),
    is_files_yo: types.maybeNull(types.string),
    is_files_yi: types.maybeNull(types.string),
    is_files_xh: types.maybeNull(types.string),
    is_files_vi: types.maybeNull(types.string),
    is_files_uz: types.maybeNull(types.string),
    is_files_ur: types.maybeNull(types.string),
    is_files_uk: types.maybeNull(types.string),
    is_files_tr: types.maybeNull(types.string),
    is_files_tl: types.maybeNull(types.string),
    is_files_th: types.maybeNull(types.string),
    is_files_tg: types.maybeNull(types.string),
    is_files_te: types.maybeNull(types.string),
    is_files_ta: types.maybeNull(types.string),
    is_files_sw: types.maybeNull(types.string),
    is_files_sv: types.maybeNull(types.string),
    is_files_su: types.maybeNull(types.string),
    is_files_st: types.maybeNull(types.string),
    is_files_sr: types.maybeNull(types.string),
    is_files_sq: types.maybeNull(types.string),
    is_files_so: types.maybeNull(types.string),
    is_files_sn: types.maybeNull(types.string),
    is_files_sm: types.maybeNull(types.string),
    is_files_sl: types.maybeNull(types.string),
    is_files_sk: types.maybeNull(types.string),
    is_files_si: types.maybeNull(types.string),
    is_files_sd: types.maybeNull(types.string),
    is_files_ru: types.maybeNull(types.string),
    is_files_ro: types.maybeNull(types.string),
    is_files_pt: types.maybeNull(types.string),
    is_files_ps: types.maybeNull(types.string),
    is_files_pl: types.maybeNull(types.string),
    is_files_pa: types.maybeNull(types.string),
    is_files_ny: types.maybeNull(types.string),
    is_files_no: types.maybeNull(types.string),
    is_files_nl: types.maybeNull(types.string),
    is_files_ne: types.maybeNull(types.string),
    is_files_my: types.maybeNull(types.string),
    is_files_mt: types.maybeNull(types.string),
    is_files_ms: types.maybeNull(types.string),
    is_files_mr: types.maybeNull(types.string),
    is_files_mn: types.maybeNull(types.string),
    is_files_ml: types.maybeNull(types.string),
    is_files_mk: types.maybeNull(types.string),
    is_files_mi: types.maybeNull(types.string),
    is_files_mg: types.maybeNull(types.string),
    is_files_lv: types.maybeNull(types.string),
    is_files_lt: types.maybeNull(types.string),
    is_files_lo: types.maybeNull(types.string),
    is_files_lb: types.maybeNull(types.string),
    is_files_la: types.maybeNull(types.string),
    is_files_ky: types.maybeNull(types.string),
    is_files_ku: types.maybeNull(types.string),
    is_files_ko: types.maybeNull(types.string),
    is_files_kn: types.maybeNull(types.string),
    is_files_km: types.maybeNull(types.string),
    is_files_kk: types.maybeNull(types.string),
    is_files_ka: types.maybeNull(types.string),
    is_files_jw: types.maybeNull(types.string),
    is_files_ja: types.maybeNull(types.string),
    is_files_iw: types.maybeNull(types.string),
    is_files_it: types.maybeNull(types.string),
    is_files_is: types.maybeNull(types.string),
    is_files_ig: types.maybeNull(types.string),
    is_files_id: types.maybeNull(types.string),
    is_files_hy: types.maybeNull(types.string),
    is_files_hu: types.maybeNull(types.string),
    is_files_ht: types.maybeNull(types.string),
    is_files_hr: types.maybeNull(types.string),
    is_files_hmn: types.maybeNull(types.string),
    is_files_hi: types.maybeNull(types.string),
    is_files_haw: types.maybeNull(types.string),
    is_files_ha: types.maybeNull(types.string),
    is_files_gu: types.maybeNull(types.string),
    is_files_gl: types.maybeNull(types.string),
    is_files_gd: types.maybeNull(types.string),
    is_files_ga: types.maybeNull(types.string),
    is_files_fy: types.maybeNull(types.string),
    is_files_fr: types.maybeNull(types.string),
    is_files_fi: types.maybeNull(types.string),
    is_files_fa: types.maybeNull(types.string),
    is_files_eu: types.maybeNull(types.string),
    is_files_et: types.maybeNull(types.string),
    is_files_es: types.maybeNull(types.string),
    is_files_eo: types.maybeNull(types.string),
    is_files_en: types.maybeNull(types.string),
    is_files_el: types.maybeNull(types.string),
    is_files_de: types.maybeNull(types.string),
    is_files_da: types.maybeNull(types.string),
    is_files_cy: types.maybeNull(types.string),
    is_files_cs: types.maybeNull(types.string),
    is_files_co: types.maybeNull(types.string),
    is_files_ceb: types.maybeNull(types.string),
    is_files_ca: types.maybeNull(types.string),
    is_files_bs: types.maybeNull(types.string),
    is_files_bn: types.maybeNull(types.string),
    is_files_bg: types.maybeNull(types.string),
    is_files_be: types.maybeNull(types.string),
    is_files_az: types.maybeNull(types.string),
    is_files_ar: types.maybeNull(types.string),
    is_files_am: types.maybeNull(types.string),
    is_files_af: types.maybeNull(types.string),
    is_files: types.maybeNull(types.string),
    lang: types.maybeNull(types.string),
    url: types.maybeNull(types.string),
    topic: types.maybeNull(types.string),
    color: types.maybeNull(types.string),
    views: types.maybeNull(types.integer),
    author: types.maybeNull(AuthorModel),
    site_name: types.maybeNull(types.string),
    files: types.optional(types.array(FileModel), []),
    comment_zu: types.maybeNull(types.string),
    comment_zh: types.maybeNull(types.string),
    comment_yo: types.maybeNull(types.string),
    comment_yi: types.maybeNull(types.string),
    comment_xh: types.maybeNull(types.string),
    comment_vi: types.maybeNull(types.string),
    comment_uz: types.maybeNull(types.string),
    comment_ur: types.maybeNull(types.string),
    comment_uk: types.maybeNull(types.string),
    comment_tr: types.maybeNull(types.string),
    comment_tl: types.maybeNull(types.string),
    comment_th: types.maybeNull(types.string),
    comment_tg: types.maybeNull(types.string),
    comment_te: types.maybeNull(types.string),
    comment_ta: types.maybeNull(types.string),
    comment_sw: types.maybeNull(types.string),
    comment_sv: types.maybeNull(types.string),
    comment_su: types.maybeNull(types.string),
    comment_st: types.maybeNull(types.string),
    comment_sr: types.maybeNull(types.string),
    comment_sq: types.maybeNull(types.string),
    comment_so: types.maybeNull(types.string),
    comment_sn: types.maybeNull(types.string),
    comment_sm: types.maybeNull(types.string),
    comment_sl: types.maybeNull(types.string),
    comment_sk: types.maybeNull(types.string),
    comment_si: types.maybeNull(types.string),
    comment_sd: types.maybeNull(types.string),
    comment_ru: types.maybeNull(types.string),
    comment_ro: types.maybeNull(types.string),
    comment_pt: types.maybeNull(types.string),
    comment_ps: types.maybeNull(types.string),
    comment_pl: types.maybeNull(types.string),
    comment_pa: types.maybeNull(types.string),
    comment_ny: types.maybeNull(types.string),
    comment_no: types.maybeNull(types.string),
    comment_nl: types.maybeNull(types.string),
    comment_ne: types.maybeNull(types.string),
    comment_my: types.maybeNull(types.string),
    comment_mt: types.maybeNull(types.string),
    comment_ms: types.maybeNull(types.string),
    comment_mr: types.maybeNull(types.string),
    comment_mn: types.maybeNull(types.string),
    comment_ml: types.maybeNull(types.string),
    comment_mk: types.maybeNull(types.string),
    comment_mi: types.maybeNull(types.string),
    comment_mg: types.maybeNull(types.string),
    comment_lv: types.maybeNull(types.string),
    comment_lt: types.maybeNull(types.string),
    comment_lo: types.maybeNull(types.string),
    comment_lb: types.maybeNull(types.string),
    comment_la: types.maybeNull(types.string),
    comment_ky: types.maybeNull(types.string),
    comment_ku: types.maybeNull(types.string),
    comment_ko: types.maybeNull(types.string),
    comment_kn: types.maybeNull(types.string),
    comment_km: types.maybeNull(types.string),
    comment_kk: types.maybeNull(types.string),
    comment_ka: types.maybeNull(types.string),
    comment_jw: types.maybeNull(types.string),
    comment_ja: types.maybeNull(types.string),
    comment_iw: types.maybeNull(types.string),
    comment_it: types.maybeNull(types.string),
    comment_is: types.maybeNull(types.string),
    comment_ig: types.maybeNull(types.string),
    comment_id: types.maybeNull(types.string),
    comment_hy: types.maybeNull(types.string),
    comment_hu: types.maybeNull(types.string),
    comment_ht: types.maybeNull(types.string),
    comment_hr: types.maybeNull(types.string),
    comment_hmn: types.maybeNull(types.string),
    comment_hi: types.maybeNull(types.string),
    comment_haw: types.maybeNull(types.string),
    comment_ha: types.maybeNull(types.string),
    comment_gu: types.maybeNull(types.string),
    comment_gl: types.maybeNull(types.string),
    comment_gd: types.maybeNull(types.string),
    comment_ga: types.maybeNull(types.string),
    comment_fy: types.maybeNull(types.string),
    comment_fr: types.maybeNull(types.string),
    comment_fi: types.maybeNull(types.string),
    comment_fa: types.maybeNull(types.string),
    comment_eu: types.maybeNull(types.string),
    comment_et: types.maybeNull(types.string),
    comment_es: types.maybeNull(types.string),
    comment_eo: types.maybeNull(types.string),
    comment_en: types.maybeNull(types.string),
    comment_el: types.maybeNull(types.string),
    comment_de: types.maybeNull(types.string),
    comment_da: types.maybeNull(types.string),
    comment_cy: types.maybeNull(types.string),
    comment_cs: types.maybeNull(types.string),
    comment_co: types.maybeNull(types.string),
    comment_ceb: types.maybeNull(types.string),
    comment_ca: types.maybeNull(types.string),
    comment_bs: types.maybeNull(types.string),
    comment_bn: types.maybeNull(types.string),
    comment_bg: types.maybeNull(types.string),
    comment_be: types.maybeNull(types.string),
    comment_az: types.maybeNull(types.string),
    comment_ar: types.maybeNull(types.string),
    comment_am: types.maybeNull(types.string),
    comment_af: types.maybeNull(types.string),
    comment: types.maybeNull(types.string),
    description_zu: types.maybeNull(types.string),
    description_zh: types.maybeNull(types.string),
    description_yo: types.maybeNull(types.string),
    description_yi: types.maybeNull(types.string),
    description_xh: types.maybeNull(types.string),
    description_vi: types.maybeNull(types.string),
    description_uz: types.maybeNull(types.string),
    description_ur: types.maybeNull(types.string),
    description_uk: types.maybeNull(types.string),
    description_tr: types.maybeNull(types.string),
    description_tl: types.maybeNull(types.string),
    description_th: types.maybeNull(types.string),
    description_tg: types.maybeNull(types.string),
    description_te: types.maybeNull(types.string),
    description_ta: types.maybeNull(types.string),
    description_sw: types.maybeNull(types.string),
    description_sv: types.maybeNull(types.string),
    description_su: types.maybeNull(types.string),
    description_st: types.maybeNull(types.string),
    description_sr: types.maybeNull(types.string),
    description_sq: types.maybeNull(types.string),
    description_so: types.maybeNull(types.string),
    description_sn: types.maybeNull(types.string),
    description_sm: types.maybeNull(types.string),
    description_sl: types.maybeNull(types.string),
    description_sk: types.maybeNull(types.string),
    description_si: types.maybeNull(types.string),
    description_sd: types.maybeNull(types.string),
    description_ru: types.maybeNull(types.string),
    description_ro: types.maybeNull(types.string),
    description_pt: types.maybeNull(types.string),
    description_ps: types.maybeNull(types.string),
    description_pl: types.maybeNull(types.string),
    description_pa: types.maybeNull(types.string),
    description_ny: types.maybeNull(types.string),
    description_no: types.maybeNull(types.string),
    description_nl: types.maybeNull(types.string),
    description_ne: types.maybeNull(types.string),
    description_my: types.maybeNull(types.string),
    description_mt: types.maybeNull(types.string),
    description_ms: types.maybeNull(types.string),
    description_mr: types.maybeNull(types.string),
    description_mn: types.maybeNull(types.string),
    description_ml: types.maybeNull(types.string),
    description_mk: types.maybeNull(types.string),
    description_mi: types.maybeNull(types.string),
    description_mg: types.maybeNull(types.string),
    description_lv: types.maybeNull(types.string),
    description_lt: types.maybeNull(types.string),
    description_lo: types.maybeNull(types.string),
    description_lb: types.maybeNull(types.string),
    description_la: types.maybeNull(types.string),
    description_ky: types.maybeNull(types.string),
    description_ku: types.maybeNull(types.string),
    description_ko: types.maybeNull(types.string),
    description_kn: types.maybeNull(types.string),
    description_km: types.maybeNull(types.string),
    description_kk: types.maybeNull(types.string),
    description_ka: types.maybeNull(types.string),
    description_jw: types.maybeNull(types.string),
    description_ja: types.maybeNull(types.string),
    description_iw: types.maybeNull(types.string),
    description_it: types.maybeNull(types.string),
    description_is: types.maybeNull(types.string),
    description_ig: types.maybeNull(types.string),
    description_id: types.maybeNull(types.string),
    description_hy: types.maybeNull(types.string),
    description_hu: types.maybeNull(types.string),
    description_ht: types.maybeNull(types.string),
    description_hr: types.maybeNull(types.string),
    description_hmn: types.maybeNull(types.string),
    description_hi: types.maybeNull(types.string),
    description_haw: types.maybeNull(types.string),
    description_ha: types.maybeNull(types.string),
    description_gu: types.maybeNull(types.string),
    description_gl: types.maybeNull(types.string),
    description_gd: types.maybeNull(types.string),
    description_ga: types.maybeNull(types.string),
    description_fy: types.maybeNull(types.string),
    description_fr: types.maybeNull(types.string),
    description_fi: types.maybeNull(types.string),
    description_fa: types.maybeNull(types.string),
    description_eu: types.maybeNull(types.string),
    description_et: types.maybeNull(types.string),
    description_es: types.maybeNull(types.string),
    description_eo: types.maybeNull(types.string),
    description_en: types.maybeNull(types.string),
    description_el: types.maybeNull(types.string),
    description_de: types.maybeNull(types.string),
    description_da: types.maybeNull(types.string),
    description_cy: types.maybeNull(types.string),
    description_cs: types.maybeNull(types.string),
    description_co: types.maybeNull(types.string),
    description_ceb: types.maybeNull(types.string),
    description_ca: types.maybeNull(types.string),
    description_bs: types.maybeNull(types.string),
    description_bn: types.maybeNull(types.string),
    description_bg: types.maybeNull(types.string),
    description_be: types.maybeNull(types.string),
    description_az: types.maybeNull(types.string),
    description_ar: types.maybeNull(types.string),
    description_am: types.maybeNull(types.string),
    description_af: types.maybeNull(types.string),
    description: types.maybeNull(types.string),
    title_zu: types.maybeNull(types.string),
    title_zh: types.maybeNull(types.string),
    title_yo: types.maybeNull(types.string),
    title_yi: types.maybeNull(types.string),
    title_xh: types.maybeNull(types.string),
    title_vi: types.maybeNull(types.string),
    title_uz: types.maybeNull(types.string),
    title_ur: types.maybeNull(types.string),
    title_uk: types.maybeNull(types.string),
    title_tr: types.maybeNull(types.string),
    title_tl: types.maybeNull(types.string),
    title_th: types.maybeNull(types.string),
    title_tg: types.maybeNull(types.string),
    title_te: types.maybeNull(types.string),
    title_ta: types.maybeNull(types.string),
    title_sw: types.maybeNull(types.string),
    title_sv: types.maybeNull(types.string),
    title_su: types.maybeNull(types.string),
    title_st: types.maybeNull(types.string),
    title_sr: types.maybeNull(types.string),
    title_sq: types.maybeNull(types.string),
    title_so: types.maybeNull(types.string),
    title_sn: types.maybeNull(types.string),
    title_sm: types.maybeNull(types.string),
    title_sl: types.maybeNull(types.string),
    title_sk: types.maybeNull(types.string),
    title_si: types.maybeNull(types.string),
    title_sd: types.maybeNull(types.string),
    title_ru: types.maybeNull(types.string),
    title_ro: types.maybeNull(types.string),
    title_pt: types.maybeNull(types.string),
    title_ps: types.maybeNull(types.string),
    title_pl: types.maybeNull(types.string),
    title_pa: types.maybeNull(types.string),
    title_ny: types.maybeNull(types.string),
    title_no: types.maybeNull(types.string),
    title_nl: types.maybeNull(types.string),
    title_ne: types.maybeNull(types.string),
    title_my: types.maybeNull(types.string),
    title_mt: types.maybeNull(types.string),
    title_ms: types.maybeNull(types.string),
    title_mr: types.maybeNull(types.string),
    title_mn: types.maybeNull(types.string),
    title_ml: types.maybeNull(types.string),
    title_mk: types.maybeNull(types.string),
    title_mi: types.maybeNull(types.string),
    title_mg: types.maybeNull(types.string),
    title_lv: types.maybeNull(types.string),
    title_lt: types.maybeNull(types.string),
    title_lo: types.maybeNull(types.string),
    title_lb: types.maybeNull(types.string),
    title_la: types.maybeNull(types.string),
    title_ky: types.maybeNull(types.string),
    title_ku: types.maybeNull(types.string),
    title_ko: types.maybeNull(types.string),
    title_kn: types.maybeNull(types.string),
    title_km: types.maybeNull(types.string),
    title_kk: types.maybeNull(types.string),
    title_ka: types.maybeNull(types.string),
    title_jw: types.maybeNull(types.string),
    title_ja: types.maybeNull(types.string),
    title_iw: types.maybeNull(types.string),
    title_it: types.maybeNull(types.string),
    title_is: types.maybeNull(types.string),
    title_ig: types.maybeNull(types.string),
    title_id: types.maybeNull(types.string),
    title_hy: types.maybeNull(types.string),
    title_hu: types.maybeNull(types.string),
    title_ht: types.maybeNull(types.string),
    title_hr: types.maybeNull(types.string),
    title_hmn: types.maybeNull(types.string),
    title_hi: types.maybeNull(types.string),
    title_haw: types.maybeNull(types.string),
    title_ha: types.maybeNull(types.string),
    title_gu: types.maybeNull(types.string),
    title_gl: types.maybeNull(types.string),
    title_gd: types.maybeNull(types.string),
    title_ga: types.maybeNull(types.string),
    title_fy: types.maybeNull(types.string),
    title_fr: types.maybeNull(types.string),
    title_fi: types.maybeNull(types.string),
    title_fa: types.maybeNull(types.string),
    title_eu: types.maybeNull(types.string),
    title_et: types.maybeNull(types.string),
    title_es: types.maybeNull(types.string),
    title_eo: types.maybeNull(types.string),
    title_en: types.maybeNull(types.string),
    title_el: types.maybeNull(types.string),
    title_de: types.maybeNull(types.string),
    title_da: types.maybeNull(types.string),
    title_cy: types.maybeNull(types.string),
    title_cs: types.maybeNull(types.string),
    title_co: types.maybeNull(types.string),
    title_ceb: types.maybeNull(types.string),
    title_ca: types.maybeNull(types.string),
    title_bs: types.maybeNull(types.string),
    title_bn: types.maybeNull(types.string),
    title_bg: types.maybeNull(types.string),
    title_be: types.maybeNull(types.string),
    title_az: types.maybeNull(types.string),
    title_ar: types.maybeNull(types.string),
    title_am: types.maybeNull(types.string),
    title_af: types.maybeNull(types.string),
    title: types.maybeNull(types.string),
    id: types.identifier,
    is_favourite: types.maybeNull(types.boolean),
    allow_iframe: types.maybeNull(types.boolean)
  })
  .views(self => ({})) // eslint-disable-line @typescript-eslint/no-unused-vars
  .actions(self => ({
    toggleFavourite: () => {
      self.is_favourite = !self.is_favourite
    }
  })) // eslint-disable-line @typescript-eslint/no-unused-vars

  /**
  * Un-comment the following to omit model attributes from your snapshots (and from async storage).
  * Useful for sensitive data like passwords, or transitive state like whether a modal is open.

  * Note that you'll need to import `omit` from ramda, which is already included in the project!
  *  .postProcessSnapshot(omit(["password", "socialSecurityNumber", "creditCardNumber"]))
  */

type MessageType = Instance<typeof MessageModel>
export interface Message extends MessageType {}
type MessageSnapshotType = SnapshotOut<typeof MessageModel>
export interface MessageSnapshot extends MessageSnapshotType {}
